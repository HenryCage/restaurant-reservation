# ============================================================
# Secrets — NEVER commit. Only the *.example files are tracked.
# ============================================================

# Shared environment / secrets
.env
.env.*
!.env.example

# Tenant registry (real client list + sheetIds) — confidential business data
tenants.json
!tenants.example.json

# Google Cloud service-account keys & private keys
# (the downloaded SA JSON must never live in the repo)
*.pem
*.p12
*service-account*.json
gcp-key*.json
credentials*.json
# GCP console's default download name is <project-id>-<12 hex chars>.json,
# which doesn't match the patterns above -- catch that shape generically...
*-????????????.json
# ...and the specific key currently sitting in this working copy.
sms-logistics-demo-45a31aed177f.json

# ============================================================
# Local SQLite data (Foundation merge sub-project — spec:
# docs/superpowers/specs/2026-07-16-foundation-merge-design.md)
# ============================================================
data/
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# ============================================================
# Reference material dumped in the repo for analysis, not project source
# ============================================================
sms-automation-main.zip

# ============================================================
# Node / build
# ============================================================
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ============================================================
# Logs
# ============================================================
logs/
*.log

# ============================================================
# OS / editor cruft
# ============================================================
.DS_Store
Thumbs.db
.idea/
.vscode/
